import styled from "styled-components";
import DetailHeader from "@/components/DetailHeader";

export default function DeviceDetail(){
    return(
        <Wrapper>
            <DetailHeader>기기이름</DetailHeader>
        </Wrapper>
    )
}

const Wrapper = styled.div`
`
